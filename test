loadstring(game:HttpGet("https://raw.githubusercontent.com/dangdizi/DiziHub/refs/heads/main/UI-D2.0.lua"))()
local DiziGui = Dizi:new()

DiziGui:setTitle("Garden H - Dizi Hub")
local AutoFarmAction = DiziGui:createAction(true)

local AutoFarmTab = DiziGui:createTab("rbxassetid://45142059", "Auto Farm")
AutoFarmTab:setAction(AutoFarmAction)

AutoFarmAction:createLabel("Tự động thu hoạch và bán", Color3.fromRGB(255,255,255))
AutoFarmAction:createHr()
local flag = false
local running = false

AutoFarmAction:createToggleSwitch("Auto Farm", function(status)
    flag = status

    if status and not running then
        running = true
        task.spawn(function()
            while flag do
                local folder = workspace:WaitForChild("ClientPlants")

for _, plant in pairs(folder:GetChildren()) do
    local count = 0
    local uuid = plant:GetAttribute("Uuid")
    -- Script generated by SimpleSpy - credits to exx#9394
    for _, obj in pairs(plant:GetChildren()) do
        if string.match(obj.Name, "^Fruit%d+$") then
            count += 1
        end
    end
    if count > 0 then
        for i = 1, count do
           local args = {
               [1] = {
                   [1] = {
                       ["GrowthAnchorIndex"] = i,
                       ["Uuid"] = uuid
                   }
              }
         }

         game:GetService("ReplicatedStorage").RemoteEvents.HarvestFruit:FireServer(unpack(args))
        end
    else
    local args = {
               [1] = {
                   [1] = {
                       ["Uuid"] = uuid
                   }
              }
         }

         game:GetService("ReplicatedStorage").RemoteEvents.HarvestFruit:FireServer(unpack(args))
    end

end

-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "SellAll"
}

game:GetService("ReplicatedStorage").RemoteEvents.SellItems:InvokeServer(unpack(args))

                task.wait(1)
            end
            running = false
        end)
    end
end)
AutoFarmAction:createHr()
AutoFarmAction:createLabel("Auto mua hạt giống", Color3.fromRGB(255,255,255))

local flagSeed = false
local runningSeed = false

AutoFarmAction:createToggleSwitch("Auto Buy Seed", function(status)
    flagSeed = status

    if status and not runningSeed then
        runningSeed = true

        task.spawn(function()
            while flagSeed do
            --- code ở đây
                task.wait(1)
            end
            runningSeed = false
        end)
    end
end)
